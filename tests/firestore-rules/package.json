{
  "name": "neon-sehir-rules-tests",
  "private": true,
  "type": "module",
  "description": "Çete sistemi Firestore security rules testleri (Firestore emülatörü gerekir)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-neon-rules \"node --test gang.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^12.15.0"
  }
}
